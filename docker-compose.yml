version: '3.7'
services:
  exist:
    image: $DOCKER_IMAGE:$DOCKER_TAG
    container_name: $EXIST_CONTAINER_NAME
    hostname: exHost
    ports:
        - $EXIST_PORT:8080
    networks:
      - www
networks:
  www:
    name: www
    driver: bridge

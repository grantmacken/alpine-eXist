version: '3.7'
services:
  exist:
    image: $DOCKER_IMAGE:$DOCKER_TAG
    container_name: $CONTAINER_NAME
    ports:
        - 8080:8080
    networks:
      - www
networks:
  www:
    name: www
    driver: bridge
